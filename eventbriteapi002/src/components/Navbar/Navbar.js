import React, { Component } from "react";
import { Header } from "semantic-ui-react";
import "./Navbar.css";

const Navbar = () => {
  return (
    <nav className="navbar">
      <span className="title">EventFinder</span>
    </nav>
  );
};

export default Navbar;
